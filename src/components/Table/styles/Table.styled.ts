import styled from "styled-components";

const StyledTable = styled.table`
  border-collapse: collapse;
  margin: 0 auto;
  font-family: 'Consolas';
  backdrop-filter: blur(6px);
  background-color: #9dc9ff2b;
`;

export default StyledTable;
